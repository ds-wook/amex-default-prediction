hydra:
  run:
    dir: log/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  output_subdir: null

defaults:
  - dataset: dataset
  - features: features
  - logger: wandb
  - model: lgbm

output:
  path: output
  name: 5fold_lightgbm_time_feature_trick.csv
  submit: sample_submission.csv
  features: features.yaml