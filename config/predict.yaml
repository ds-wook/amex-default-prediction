hydra:
  run:
    dir: log/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  output_subdir: null

defaults:
  - dataset: dataset
  - features: features
  - logger: wandb
  - model: lgbm

output:
  path: output
  name: 5fold_catboost_lag_features_seed131.csv
  submit: sample_submission.csv
  features: features.yaml
