hydra:
  run:
    dir: log/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  output_subdir: null

defaults:
  - dataset: dataset
  - features: features
  - logging: wandb
  - models: lgbm

output:
  path: output
  name: 5fold_lightgbm_dart_seed42.csv
  submit: sample_submission.csv
  features: features.yaml